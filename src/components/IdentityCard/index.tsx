import { BrandMark } from '@/components/BrandMark';
import { StudentPhoto } from '@/components/StudentPhoto';
import { formatDate } from '@/utils/masks';
import type { IdentityCardProps } from './types';
const Info = ({ label, value, wide }: { label: string; value?: string; wide?: boolean }) => <div className={wide ? 'col-span-2' : ''}><dt className="text-[10px] font-bold uppercase tracking-[.11em] text-slate-400">{label}</dt><dd className="mt-1 truncate text-[15px] font-medium text-slate-600">{value || '—'}</dd></div>;
export function IdentityCard({ data, cardRef }: IdentityCardProps) { return <article ref={cardRef} aria-label="Prévia da carteirinha" className="card-print relative mx-auto aspect-[.62] w-full max-w-[375px] overflow-hidden rounded-[3px] border border-slate-200 bg-white p-3 shadow-card"><div className="relative h-full overflow-hidden border border-slate-100 px-6 pb-6 pt-7"><div aria-hidden className="absolute -right-20 -top-16 h-52 w-52 rounded-full border-[30px] border-brand/5" /><div aria-hidden className="absolute -bottom-28 -left-20 h-48 w-48 rounded-full border-[22px] border-cyan-500/5" /><div className="relative"><BrandMark /><div className="mx-auto mt-2 h-px w-20 bg-gradient-to-r from-transparent via-brand/40 to-transparent" /><p className="mt-3 text-center text-[9px] font-bold uppercase tracking-[.2em] text-brand">Identidade estudantil</p><div className="mt-4"><StudentPhoto src={data.photoUrl} name={data.fullName} /></div><dl className="mt-7 grid grid-cols-2 gap-x-4 gap-y-4 leading-tight"><Info label="Nome" value={data.fullName} wide /><Info label="CPF" value={data.cpf} /><Info label="Nascimento" value={formatDate(data.birthDate)} /><Info label="Curso" value={data.course} wide /><Info label="Matrícula" value={data.enrollment} /><Info label="Validade" value={formatDate(data.validUntil)} /></dl></div><footer className="absolute bottom-7 left-6 right-6 border-t border-slate-100 pt-4 text-center text-[10px] leading-relaxed text-slate-400">Válida mediante apresentação de documento<br />de identidade oficial com foto.</footer></div></article>; }

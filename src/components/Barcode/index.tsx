export function Barcode({ value }: { value: string }) { const bars = Array.from(value || '000000000000', (char, index) => ({ width: (Number(char) % 3) + 1, key: `${char}-${index}` })); return <div aria-label={`Código de barras ${value}`} className="flex h-10 items-stretch gap-px">{bars.map(({ width, key }, index) => <i key={key} style={{ width: `${width}px`, opacity: index % 5 === 0 ? .65 : 1 }} className="bg-slate-900" />)}</div>; }
